<div class="col-lg-12">
  <div class="row form">
    <div class="col-xs-12 col-sm-12 col-md-12">
      <%= link_to "Back to Top", "/admin" %>
    </div>
    <% @people.each do |person| %>
      <div class="col-xs-6 col-sm-6 col-md-6">
        <% if person.avatar.present? %>
          <%= image_tag person.avatar.url(:medium) %>
        <% else %>
          <%= image_tag("no_image.png", :size => "248×248") %>
        <% end %>
      </div>
      <div class="col-xs-6 col-sm-6 col-md-6">
        <h3><%= person.name %></h3>
        <h4>keywords</h4>
        <% person.keywords.each do |keyword| %>
          <p>◆ <%= keyword.name %></p>
        <% end %>
      </div>
      <div class="col-xs-12 col-sm-12 col-md-12">
        <br>
        <p><%= person.detail %></p>
      </div>
      <div class="col-xs-12 col-sm-12 col-md-12">
        <%= link_to "編集", edit_person_path(person), :class => "btn btn-info normal" %>
      </div>
    <% end %>
    <%= paginate(@people) %>
  </div>
</div>
